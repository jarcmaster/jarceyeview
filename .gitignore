# Entorno virtual de Python
.venv/
venv/
__pycache__/
*.py[cod]

# Secretos (NUNCA subir). Solo se versiona .env.example
.env
.env.*
!.env.example

# Sistema
.DS_Store
Thumbs.db

# Vídeos locales servidos por el frontend (no subir al repo)
frontend/assets/*.mp4

# Caché local del backend (datos de calles OSM/Overpass) — no subir
.cache/
